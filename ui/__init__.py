"""Terminal-styled on-screen popup that mirrors listener/vad_listener.py's
pipeline state (listening / processing / speaking / error) while EKKO is
active. Purely visual, no dependency back on the rest of the repo, and no
effect on anything if never wired in: see voice_ui.py's VoiceUI.
"""
