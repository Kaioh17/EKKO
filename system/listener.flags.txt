# Extra CLI flags passed to listener\vad_listener.py by the background
# task, one flag (or flag + value) per line. Blank lines and lines
# starting with # are ignored.
#
# Read once when run_listener.ps1 starts. To pick up a change here,
# restart the task: system\stop.ps1 then system\start.ps1 (or wait for
# the next logon).
#
# Live as of 2026-08-10: the background task can now run matched
# handler scripts for real. Add --no-execute back on a line below to
# return to routing/logging/speaking only, no execution.

--no-verify
